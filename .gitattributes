# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Go files are always LF.
*.go text eol=lf
go.mod text eol=lf
go.sum text eol=lf

# Treat binaries as binary.
*.png binary
*.gif binary
*.jpg binary
